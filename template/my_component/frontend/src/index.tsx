import React from "react"
import ReactDOM from "react-dom"
import DispatcherComponent from "./DispatcherComponent"

ReactDOM.render(
  <React.StrictMode>
    <DispatcherComponent />
  </React.StrictMode>,
  document.getElementById("root")
)
